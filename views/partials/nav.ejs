  <% if (!user) { %>
    <link rel="stylesheet" href="/stylesheets/siwg.css">
  <% } %>
</head>
<body>
  <nav>
    <a href="/lakes">
      <i class="fas fa-magnifying-glass" id="home-icon"></i>
    </a>
    <form action="/fishes/new" method="GET">
      <button type="submit" id="nav-btn">Add a Fish</button>
    </form>
    <button id="user-nav-btn"> <a href="/profiles">
      <i class="fas fa-user-group"></i>
    </a>
  </button>
    <form action="/lakes/new" method="GET">
      <button type="submit" id="nav-btn">Add a Lake</button>
    </form>
    <% if (user) { %>
      <a href="/auth/logout">
        <button type="submit">Logout</button>
      </a>
    <% } else { %>
      <a href="/auth/google" class="siwg-link">
        <button type="submit" class="siwg">
          <img id="siwg-image" alt="Sign in with Google" />
        </button>
      </a>
    <% } %>
  </nav>

  
